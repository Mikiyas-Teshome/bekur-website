import BlogLayout from "@/components/blogPage/Blogs";

export const metadata = {
  title: "Blog — Operator insights",
  description:
    "Logic-first takes on automation, AI spend caps, and reclaiming partner hours for professional service firms.",
};

export default function BlogPage() {
  return <BlogLayout />;
}
